# frozen_string_literal: true

RSpec.describe EasyWeather do
  it "has a version number" do
    expect(EasyWeather::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
